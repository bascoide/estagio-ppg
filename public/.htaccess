Options -Indexes
RewriteEngine On

# Servir ficheiros e diretorias existentes diretamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirecionar tudo o resto para index.php
RewriteRule ^ index.php [QSA,L]
